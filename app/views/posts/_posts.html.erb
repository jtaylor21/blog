<% @posts.each do |post| %>

	<h2><%= link_to post.title, post, :class => "override" %></h2>
	<p><h7>Posted on <%= time_tag(post.created_at, :format => '%B %d, %Y') %></h7></p>
	<%= post.body.html_safe %>
	<br />
	<br />
	
<% end %>

<%= link_to "RSS Feed", formatted_articles_url(:rss) %>