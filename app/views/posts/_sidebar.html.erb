<div id="sidebar_title">RECENT POSTS</div>

<div id="sidebar_content">
<% @recent_posts.each do |post| %>
	<%= link_to post.title, post %>
	<br />
<% end %>

<br />

<div id="sidebar_title">RECENT TWEETS</div>

<div id="tweets">
<% tweets.each do |t| %>
    <%= markdown(tweet_handle_parser(t.text)) %></p>
<% end %>
</div>



<% if logged_in? %>
Logged in as <%= link_to 'Logout', sessions_destroy_url, :method => :delete %>
<p><%= link_to "Settings", settings_url %></p>
<% else %>
<p><%= link_to 'Login', login_page_url %></p>
<%#= link_to 'Login with Twitter', '/auth/twitter' %>
<% end %>

<% if logged_in? %>
<p><%= link_to 'New Post', new_post_url %></p>
<% else %>
<% end %>

</div>

